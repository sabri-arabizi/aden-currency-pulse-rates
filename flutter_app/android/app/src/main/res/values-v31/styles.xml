<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!--
      نسخة Android 12+ (API 31+): النظام يستخدم SplashScreen API الجديد
      ويعرض الشعار على لون الخلفية تلقائياً قبل أول إطار لـ Flutter.
    -->
    <style name="LaunchTheme" parent="@android:style/Theme.Light.NoTitleBar">
        <!-- خلفية بيضاء متناسقة مع صورة شاشة البداية (الأيقونة الافتراضية
             للتطبيق تُعرض تلقائياً على Android 12+). -->
        <item name="android:windowSplashScreenBackground">#FFFFFF</item>
        <item name="android:windowBackground">@drawable/launch_background</item>
    </style>

    <style name="NormalTheme" parent="@android:style/Theme.Light.NoTitleBar">
        <item name="android:windowBackground">?android:colorBackground</item>
    </style>
</resources>