# Backend for local development

This is a minimal Express backend used for local development and testing the Android/web UI.

Setup:

```bash
cd backend
npm install
```

Run:

```bash
npm run dev    # uses nodemon
# or
npm start
```

Endpoints:
- `GET /api/health`
- `GET /api/rates`
- `GET /api/gold`
